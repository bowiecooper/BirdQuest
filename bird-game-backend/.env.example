PORT=3001
# Google Cloud Vision API (optional - for bird detection fallback)
# GOOGLE_CLOUD_API_KEY=your_google_cloud_api_key_here

# Note: iNaturalist computer vision API doesn't require authentication
# It's used as the primary bird identification service